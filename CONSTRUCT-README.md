<!-- Generated by .claude/scripts/butterfreezone-construct-gen.sh -->
<!-- Canonical source: construct.yaml · do not edit by hand -->

# the-orchard

> Post-export operational running of NFT collections

## About

| field | value |
|---|---|
| slug | `the-orchard` |
| version | `0.1.0` |
| schema_version | `3` |
| author | 0xHoneyJar |
| license | MIT |

Runs collections after they're alive. Storage as soil, customization as grafting, cultivation cycles as harvest. Persona: LUTHER BURBANK.

## Personas

- `@ARCHITECT` → [`identity/ARCHITECT.md`](identity/ARCHITECT.md)
- `@LUTHER_BURBANK` → [`identity/LUTHER_BURBANK.md`](identity/LUTHER_BURBANK.md)

## Skills

- `cultivating-cycle` — Orchestrate one cultivation cycle for a collection — plan → drop/harvest/graft → retro. Top-level act-skill of the events cluster. Scopes a coherent operational rhythm rather than ad-hoc moments.
- `grafting-traits` — Apply a community-input-driven trait change to a token. Updates metadata + image, re-pins, prepares the protocol-side contract call. The act-skill of the customization cluster.
- `harvesting-input` — Collect community proposals during an open-input window — Discord threads, Twitter polls, governance votes — and structure them into a graft-proposal queue. Input collection in service of cultivation.
- `migrating-storage` — Lift a collection's metadata + images off a defunct pinner (e.g. the dead mibera metadata service) onto Freeside + IPFS dual-pin. Recovery skill — the construct's first cultivation event.
- `pinning-to-freeside` — Upload metadata JSON and image assets to Freeside (Honey Jar's own infra) and return immutable URIs. Vendor implementation for the primary pin under the dual-pin policy.
- `pinning-to-ipfs` — Pin metadata + image to IPFS via the configured pinner (Pinata, nft.storage, web3.storage, or self-hosted). Vendor implementation for the fallback pin under the dual-pin policy. Decentralized survival layer.
- `retrospecting-cycle` — Close a cultivation cycle by authoring its retro — what landed, what surprised, what next-cycle should know. Composes with KEEPER for cultural learning. The cycle's load-bearing output.
- `scheduling-drops` — Plan a time-locked operational event — mint, graft drop, harvest event — with substrate pre-flight, broadcast composition, and on-landing verification. Operational moment within a cultivating-cycle.
- `tending-storage` — The orchard's primary skill. Tends the storage substrate — dual-pin policy (Freeside primary + IPFS fallback), pin verification, regeneration when drift surfaces. Every other skill in the construct depends on this substrate being sound.
- `updating-image` — Replace a token's image — composes with the-mint when regeneration is needed — re-pin both image and metadata, update the metadata's image-hash reference. Image specialization of grafting-traits.
- `updating-metadata` — Edit a token's JSON metadata in-place — attribute swap, name correction, description update — and re-pin under dual-pin policy. JSON-only specialization of grafting-traits.
- `verifying-mutation` — Confirm a graft landed both on-chain and on the pinner. Reads tokenURI from the contract, resolves it, hashes the result, and confirms it matches the mutation-log entry. Read-only Verdict emitter.
- `verifying-pin` — Probe a pinned URI (Freeside or IPFS) and confirm it resolves to content matching the recorded hash. Returns a Verdict the dual-pin policy enforcement reads.

## Commands

- `/tend` — Tending pass for the-orchard — verify dual-pin health, surface drift, plan or close cultivation cycles, run grafts, schedule drops. Routes to skills based on what the operator asks for.

## Composes with

_None declared._

## Streams

**Reads**:

- Intent
- Signal
- Verdict

**Writes**:

- Artifact
- Signal
- Verdict

## Grimoires read/write (SEED §12)

**Reads from**:

- `grimoires/{product}/context/`
- `grimoires/{product}/feedback/keeper/`
- `grimoires/{product}/flows/`
- `grimoires/{product}/intents.yaml`

**Writes to**:

- `grimoires/{product}/cultivations/`
- `grimoires/{product}/mutations/`
- `grimoires/{product}/pins/`
- `grimoires/{product}/proposals/`

> The grimoire path IS the interface — constructs writing to the same path compose automatically.

## Install

```bash
/constructs install the-orchard
```

## Provenance

- Canonical spec: `construct.yaml`
- Generator: `.claude/scripts/butterfreezone-construct-gen.sh`
- Conventions: typed streams (Signal/Verdict/Artifact/Intent/Operator-Model) · composition pipes · grimoires-as-interface
